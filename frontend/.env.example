VITE_API_BASE_URL=http://localhost:8000

# Ships to the browser (Vite inlines VITE_-prefixed vars into the client
# bundle) -- only safe if this key has domain-locking configured on the Ola
# Maps dashboard for the domains this app is served from. See README.
VITE_OLA_MAPS_API_KEY=
